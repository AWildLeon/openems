# direnv .envrc to enter the flake devShell for this repository
# Uses the devShell attribute defined in flake.nix

# Explicitly use the flake devShell for x86_64-linux
use flake .#devShells.x86_64-linux.default

